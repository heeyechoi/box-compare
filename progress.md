# 박스 크기 비교 웹앱 — 진행 상황

가로·세로·높이를 입력하면 CSS 3D로 입체 박스를 그려주고, 여러 박스와 기준 물체(아이폰·지폐·신용카드 등)를 **같은 축척의 한 3D 장면에 실제 비율로 배치**해서 크기 감각을 비교하는 도구. 서버·빌드·외부 라이브러리 전혀 없이 **파일 하나**(`index.html` — HTML+CSS+vanilla JS)로 동작한다.

**배포 주소**: https://heeyechoi.github.io/box-compare/ (GitHub Pages, `main` 브랜치 정적 서빙 — Actions 빌드 없이 저장소 루트를 그대로 서빙)
**저장소**: https://github.com/heeyechoi/box-compare (public)

## 아키텍처 요약

- **파일 구조**: `index.html` 한 개(현재 806줄)에 `<style>` + `<script>` 전부 포함. 빌드 도구 없음 — 로컬에서도 더블클릭으로 `file://` 그대로 열려서 동작.
- **상태**: 전역 `let S = { items:[...], unit, mode, base, view:{rx,ry,zoom,panX,panY,tool} }`. `items[]`의 각 원소는 `{ id, name, w, d, h, color, kind:'box'|'ref', visible, ox, oz }` — 내부 치수 단위는 **항상 mm**, 화면 표시할 때만 `UNITS[S.unit]`으로 변환(`toMM`/`fromMM`). `save()`가 200ms 디바운스로 `localStorage['box-compare-v1']`에 전체 `S`를 저장.
- **CSS 3D 렌더러(라이브러리 없음)**: `#stage`에 `perspective`, `#world`에 `rotateX/rotateY/scale3d/translate3d(pan)`. 박스 1개 = `.box`(빈 앵커, `transform-style:preserve-3d`) 안에 `.face` 6장(`backface-visibility:hidden`로 뒷면 자동 컬링) + 빌보드 라벨. **깊이 정렬은 브라우저의 `preserve-3d` 자동 정렬을 믿지 않고 매 프레임 직접 계산**(`applyView()`가 카메라 회전각으로 각 박스 중심의 카메라-공간 깊이를 구해 화가 알고리즘으로 DOM 재정렬, `lastOrder` 캐시로 순서 안 바뀌면 재정렬 생략).
- **축척은 카메라 조작과 완전히 분리**: `layout()`이 계산하는 `k`(mm→px 배율)는 보이는 아이템들의 바운딩 박스로만 정해지고, 휠 줌은 `#world`의 `scale3d`만 건드림 — 회전·줌·이동 어떤 조작을 해도 **박스 간 상대 크기 비율은 절대 안 깨짐**(이 앱의 존재 이유이므로 여기 손대지 말 것).
- **배치 모드 2종**: `나란히`(X축으로 늘어놓기, 모든 박스 바닥을 y=0 정렬) / `겹쳐보기`(뒤·왼쪽·아래 모서리 공유 + 반투명 렌더로 안에 든 것처럼 보이게).
- **자유 위치 이동**: 박스를 직접 드래그하면 바닥 평면(XZ)에서 자유 이동(`item.ox/oz`에 오프셋 누적, 높이는 항상 바닥 고정). `screenDeltaToGround()`가 화면 픽셀 이동량을 현재 카메라 회전각의 역행렬로 풀어서 mm 단위 바닥 이동량으로 변환(`rx≈0`일 때 특이점 가드 있음). `reposition()`은 `layout()`+DOM 재생성 없이 좌표만 갱신(드래그 중 60fps 대응).
- **회전(치수 스왑) 버튼**: 카드마다 가로⇄세로/세로⇄높이/가로⇄높이 3개 — 실제 회전이 아니라 두 치수값을 맞바꾸는 방식(물리적으로 박스를 눕힌 것과 동일, 부피/겉넓이/대각선 불변, `fitCount`는 원래도 6가지 회전 조합을 다 시도하므로 결과 불변 — Node로 검증됨).
- **카메라 조작 2모드**: HUD의 `이동`/`회전` 토글이 **배경** 드래그 동작을 바꿈(박스 자체를 드래그하는 건 모드와 무관하게 항상 개별 이동). **스페이스바를 누르는 동안만 일시적으로 이동 모드**로 전환(`effTool()`), 떼면 선택해둔 모드로 복귀 — 입력창 포커스 중엔 무시, 창 포커스 잃으면 자동 해제.
- **호버 프리뷰**: 클릭 전 마우스가 올라간 박스를 미리 밝게 강조(`setHover`, `e.target.closest('.box')`로 실제 클릭 시 잡힐 박스와 동일한 판정 로직을 재사용 — 항상 일치 보장).
- **CSS 3D 관련 함정 두 가지(재발 방지용 기록)**:
  1. `filter`가 걸린 요소는 `transform-style:preserve-3d`가 무시되고 평평해진다 — 드래그/호버 강조(`brightness()`)는 반드시 `.box` 자신이 아니라 낱장 `.face`에만 걸 것.
  2. 드래그 중 다른 박스 라벨 텍스트가 파랗게 선택되는 문제 → `#stage`에 `user-select:none` 전체 적용으로 해결.
  3. 그림자(`.shd`)에는 위치 초기화용 부드러운 트랜지션이 있는데, 드래그 중엔 박스(`.box.is-drag`)뿐 아니라 **그림자에도 같이 `is-drag`를 붙여 transition을 꺼야** 잔상이 안 생김.
- **공유 링크 2단계**:
  - **긴 링크(기본, 항상 동작)**: `🔗 공유 링크` 버튼이 `{items,unit,mode,base}`를 JSON→UTF8-safe base64url로 인코딩해 `#d=...`에 담음. `ox/oz`는 소수 2자리로 반올림 후 인코딩(불필요한 부동소수점 자릿수로 링크가 길어지는 것 방지). 완전히 자기완결적 — 서버·네트워크 없이 그 순간의 데이터를 링크 자체에 통째로 담아 전달, `file://`에서도 동작.
  - **짧은 링크(수동, 내가 CLI로 생성)**: 같은 payload를 GitHub Gist에 `box-compare.json` 파일로 올리고 `?g=<gist id>` 형태로 전달. 페이지 로드시 `loadFromGist()`가 `https://api.github.com/gists/<id>`를 비동기 fetch해서 반영(공개 Gist라 읽기는 인증 불필요, 실패 시 조용히 기존 상태 유지). **Gist 생성은 인증이 필요해서 앱 안의 버튼이 자동으로 만들 수는 없음** — 사용자가 요청하면 내가 `gh gist create`로 만들어서 짧은 링크를 전달하는 수동 흐름. (하단 "다음에 할 일" 참고 — 사용자 소유 토큰을 브라우저에 저장해 버튼이 직접 Gist를 만들게 하는 안을 논의했으나 아직 미구현.)
  - `location.hash`(`#d=`)가 `localStorage`보다 우선 적용됨 — 공유받은 링크를 열면 항상 그 내용부터 보여줘야 하므로.
- **기준 물체 프리셋**: `PRESETS` 배열(코드 상단)에 신용카드·만원권·아이폰·A4·캔·생수병·노트북·신발상자·택배1호·사람(170cm) 등 10종, mm 기준 근사치. 드롭다운으로 추가되면 이후 일반 박스와 동일하게 치수 수정 가능.
- **분석 표**: 보이는 아이템 전체을 표로 — 정규화 비율(`1:1.5:2.4`), 부피(mm³→상황에 맞게 cm³/L/m³ 자동 전환), 겉넓이, 바닥 면적, 공간 대각선, 기준 대비 배수, 기준 박스에 몇 개 들어가는지(`fitCount`, 6가지 회전 조합 중 최댓값). 표 행 클릭으로 기준(baseline) 변경.
- **단위 전환**: mm/cm/inch. 내부 값은 항상 mm라서 왕복 전환해도 반올림 오차 누적 없음(Node로 검증).

## 배포 방식 메모

- `gh` CLI는 시스템에 설치돼 있지 않아 `~/.local/bin/gh`에 무설치 바이너리로 직접 넣어둠(brew 없음). `PATH="$HOME/.local/bin:$PATH"`를 매번 붙여줘야 셸에서 보임.
- GitHub 로그인은 `gh auth login -h github.com -p https -w`(디바이스 플로우, 브라우저에서 코드 입력) — 사용자 본인 계정(`heeyechoi`)으로 완료됨, `git config user.name/email`은 저장소 로컬로만 설정(글로벌 미설정 상태 유지).
- Pages는 GitHub Actions 빌드가 아니라 **레거시 방식**(`gh api repos/.../pages -f source[branch]=main -f source[path]=/`)으로 켬 — push 후 빌드 완료까지 `repos/.../pages/builds/latest`의 `status`가 `built`가 될 때까지 폴링해서 확인.

## 검증 방식

브라우저 자동화 도구가 연결되어 있지 않아(사용자가 Claude in Chrome 확장 설치를 보류함) **모든 로직은 Node로 스크립트의 순수 함수 부분만 추출해 단위 테스트로 검증**하고, 실제 3D 렌더링/드래그 손맛은 사용자가 직접 확인하는 방식으로 진행해왔음. 지금까지 만든 임시 테스트 스크립트(`/tmp/bc_test*.js`, `/tmp/bc_share_test*.js`, `/tmp/bc_gist_*.js` 등, 세션 스크래치 영역이라 세션 종료 시 사라짐)가 커버한 항목:
- 레이아웃/축척: 상대 크기 비율 유지, 나란히/겹쳐보기 배치 규칙, 바닥 정렬, 숨김·0치수 제외
- 단위 왕복 무손실(mm↔cm↔in), 부피/겉넓이 자동 단위 전환
- 분석: `fitCount`(회전 조합 포함), 기준 대비 배수
- 위치 드래그: 화면 픽셀→바닥 mm 역변환 왕복 정확도(여러 회전각), rx≈0 특이점 가드, 오프셋이 자동배치 축척(k)에 영향 안 줌
- 회전(치수 스왑): 부피/겉넓이/대각선 불변, `fitCount` 스왑 전후 동일
- 공유 링크: base64url 왕복(한글 이름 포함), 해시 우선순위, Gist 비동기 로드, `ox/oz` 반올림

## 다음에 할 일 / 열린 논의

- **자동 짧은 링크 버튼(미구현)**: 사용자가 "버튼 하나로 자동 짧은 링크"를 요청 → 익명으로는 안전하게 불가(Gist 생성엔 인증 필요, 내 토큰을 페이지에 넣으면 소스보기로 탈취 가능)라고 설명함. 대안으로 **"사용자 본인의 GitHub PAT(gist 권한만)를 브라우저 localStorage에 한 번만 저장 → 버튼이 그 토큰으로 직접 Gist 생성/갱신(PATCH로 같은 gist 재사용해서 링크 고정) → 실패시 기존 긴 링크로 폴백"** 방식을 제안한 상태. 사용자 확인/승인 대기 중 — 다음에 이어서 구현할 것.
- 현재 짧은 링크가 필요하면 사용자가 요청 → 내가 `gh gist create`로 수동 생성 → `?g=<id>` 링크 전달하는 흐름으로 임시 운영 중.
